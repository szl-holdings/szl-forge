# Field-leader bridge (EVALUATION)

Owner: `szl-holdings/szl-forge`.
Companion second-reader: `tools/szl_payload_contract.py`.
Companion dream-gate: `tools/szl_predicate_and.py`.
Do not replace the collector or any publisher.

Scraped 2026-09-20 and bound to doctrine v11:

| Source | Take | SZL tweak |
| --- | --- | --- |
| Anthropic pacing measurements | Report only what a developer can measure today | Pacing card. Autonomy subset = false. |
| Traccia evidence layers | State > artifact > behavior > self-report | `CLAIM_OUTRANKS_EVIDENCE` |
| τ-bench | pass@k ≠ pass^k | ALTK refuse-collapse + consistency gap |
| Evaluator Bench | Evaluate the evaluator | Collector ≠ second-reader, no network, cannot authorize |
| Observable-AI SRE | Context / policy / outcome + one trace | Telemetry layers |
| HF signed kernels | Untrusted publishers stay explicit | EVALUATION, no trusted-publisher claim |
| Linear / Stripe / Vercel | One accent; chips mean something | Chip is a predicate. LIVE chip is forbidden. |
| TypeSafe System One | Typed judgments, code owns workflow | `classify()` not a generated essay |

`production_authorization` is always false.
Passing tests do not qualify the estate.
Keep draft. Do not merge from a cockpit. Do not dispatch a publisher.
