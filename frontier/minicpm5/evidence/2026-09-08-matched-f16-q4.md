# Same-worker F16/Q4 execution: equal totals, different failures

## Observed job, not a promised benchmark

- Provider job: https://huggingface.co/jobs/SZLHOLDINGS/6a9ff695b012ba1d5b8f2b37
- Created: 2026-09-08T11:50:45.060Z; finished: 2026-09-08T11:54:17.203Z.
- Provider terminal status: ERROR/exit 2, the intentional completed quality/parity failure, not timeout.
- One cpu-upgrade job; 900-second hard limit; two sequential evaluations; no retries or secrets.
- Executed Forge source: `04c68889c996b74c3b131190abd5f87b157a520c`.
- Matched-runner SHA-256: `b79abb7c77aa6e4cbd1988cb2e90ead8a3b08410fab5491f16979b363d981802`.
- Existing GGUF evaluator SHA-256: `e94f91230c25e712ae77b076a3bfabe60704ae70b244c8c8b12fb781dfa9a573`.
- Original grader SHA-256: `b1f750253004d43fd44ead3581245920867ee775b9388ff3eec002af80a29af7`.

Official F16 then Q4_K_M bytes were downloaded only to ephemeral evaluation
storage, SHA-verified, and run with the same llama.cpp binary/archive, original
system prompt and suite, request protocol, four server threads, CPU-only setting,
4096 context and 96-token maximum. The native listener was authenticated and
loopback-only. No model call had tool authority. The source wrapper verified the
three Git blob identities before execution. Its post-run verifier exited 0.

## Actual result

| Observation | F16 | Q4_K_M |
| --- | --- | --- |
| Completed probes | 12/12 | 12/12 |
| Correct probes | 9/12 | 9/12 |
| Model outcome | SMOKE_FAIL | SMOKE_FAIL |
| Failed probe IDs | 00, 04, 10 | 04, 06, 10 |
| Roundtrip p50 | 6403.305 ms | 2657.402 ms |
| Roundtrip p95 | 7103.703 ms | 3206.378 ms |
| Native server resident high-water | 5302505472 bytes | 2777247744 bytes |

Q4 recovered probe_00 and regressed probe_06 against F16 on the same worker.
Ten of twelve output hashes match. All input token counts match, which does not
prove identical token sequences. Both variants over-abstained on the two
instruction-like-note lookup probes. F16's twelve output hashes also match the
historical A10G result, but that cross-hardware observation is not a causal test.

The worker reported AMD EPYC 7R13, Linux x86-64 and Python 3.12.3. Its host-visible
cpuCount=64 is NOT a claim that 64 CPUs were allocated. Both server commands used
four threads. Timing includes cold requests and fixed F16-first ordering. Memory
is the native process high-water, not total host/container footprint. No TTFT,
energy, billed-cost, statistical speedup or production-performance claim is made.

## Integrity and representation

The paired JSON values were transcribed from the completed job's chunked base64
log record. The complete canonical SHA-256 and both nested variant/source digests
were independently recomputed and matched the job-emitted originals before
admission. This is a digest-verified transcription, not a directly downloaded
untouched artifact or a signed attestation. No unknown value or credential was
inferred. The repository stores deterministic gzip of that canonical JSON plus
one trailing newline (mtime zero); the companion regression test checks the
archive bytes, decompressed bytes, full record and nested semantics.

- Archive SHA-256: `324037fd2548507460e94890c866123c6606a2032432bf20e0d049c9f22da7a4`
- Decompressed file SHA-256: `e4fb24662541b61491939f9c1fccd848105256a37bf323c31b1ed8f9caeac9c1`
- Paired record SHA-256: `f514192cf45013e55ad75d748554bce1769dbcac9ffdfbdacea00c065543fef0`
- F16 record SHA-256: `1ea25d896a149f62533b45be35a8c1f59badb66fd345a06e576ae8e4a092785f`
- Q4 record SHA-256: `1c38ba21ec56e65aa586c56099221cfbde7c22c8afcaf133ab1086f1a7cb06ea`

```bash
python -c "import gzip,pathlib; p=pathlib.Path('frontier/minicpm5/evidence/2026-09-08-matched-f16-q4.json.gz'); pathlib.Path('/tmp/matched.json').write_bytes(gzip.decompress(p.read_bytes()))"
python -m inference.minicpm5_matched verify --record /tmp/matched.json
```

## Admission boundary

EXECUTED means two complete same-worker observations, not a passing model.
`quantizationIsolationVerified=false` remains correct: embedded template/tokenizer
and conversion lineage, ordering effects, independent held-out quality and native
tool parsing still need evidence. All results remain unsigned, modelOperational=false,
runtimeQualified=false, publicationEligible=false and productionDisposition=HOLD.
The original historical failure files, suite and grader were not modified.

GitHub -> Hugging Face -> a-11-oy.com -> a11oy.net. Forge owns this evidence;
Frontier records its identity and limitations. Product/proof owners must not
turn EXECUTED into LIVE or deployment authority. No website or public runtime
was changed by the experiment; no additional compute is needed to replay it.
